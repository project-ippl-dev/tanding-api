version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "./postgresql/query/"
    schema: "./postgresql/migration/"
    engine: "postgresql"
    emit_prepared_queries: false
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: true
    emit_json_tags: true